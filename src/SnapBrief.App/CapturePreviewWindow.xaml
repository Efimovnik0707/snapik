<Window x:Class="SnapBrief.App.CapturePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SnapBrief.App"
        xmlns:controls="clr-namespace:SnapBrief.App.Controls"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{local:UiText Просмотр снимка}" AutomationProperties.Name="{local:UiText Просмотр снимка}"
        Width="1100" Height="760" MinWidth="720" MinHeight="500"
        WindowStyle="None" ResizeMode="CanResize" Background="#101318"
        ShowInTaskbar="True" WindowStartupLocation="Manual">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="46" ResizeBorderThickness="7" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="PreviewButton" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="32" />
            <Setter Property="Padding" Value="9,0" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Foreground" Value="#E9EDF4" />
            <Setter Property="Background" Value="#242A33" />
            <Setter Property="BorderBrush" Value="#3A4451" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="controls:ButtonChrome.HoverBackground" Value="#323B48" />
            <Setter Property="controls:ButtonChrome.PressedBackground" Value="#1D222A" />
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{Binding Path=(controls:ButtonChrome.HoverBackground), RelativeSource={RelativeSource TemplatedParent}}" /><Setter TargetName="Chrome" Property="BorderBrush" Value="#657388" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{Binding Path=(controls:ButtonChrome.PressedBackground), RelativeSource={RelativeSource TemplatedParent}}" /></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Chrome" Property="BorderBrush" Value="#7AB8FF" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.42" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PreviewPrimaryButton" TargetType="Button" BasedOn="{StaticResource PreviewButton}">
            <Setter Property="Background" Value="#2F8CFF" />
            <Setter Property="BorderBrush" Value="#2F8CFF" />
            <Setter Property="controls:ButtonChrome.HoverBackground" Value="{StaticResource AccentHoverBrush}" />
            <Setter Property="controls:ButtonChrome.PressedBackground" Value="{StaticResource AccentPressedBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource PreviewButton}">
            <Setter Property="Width" Value="32" />
            <Setter Property="Padding" Value="8" />
        </Style>
        <Style x:Key="PreviewTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="#EEF2F8" />
            <Setter Property="Background" Value="#1C222B" />
            <Setter Property="BorderBrush" Value="#3A4451" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="9,7" />
            <Setter Property="CaretBrush" Value="#7AB8FF" />
            <Setter Property="SelectionBrush" Value="#315CF5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Field" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Field" Property="BorderBrush" Value="#7AB8FF" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="WindowShell" Background="#F4101318" BorderBrush="#3A4451" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="46" /><RowDefinition Height="*" /></Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#171B22">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                    <Border Width="24" Height="24" CornerRadius="12" Background="#2F8CFF">
                        <TextBlock x:Name="CaptureLabel" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="11" />
                    </Border>
                    <TextBlock Margin="9,0,0,0" Foreground="#EEF2F8" FontWeight="SemiBold" VerticalAlignment="Center" Text="{local:UiText Просмотр снимка}" />
                    <TextBlock x:Name="ImageSizeText" Margin="9,0,0,0" Foreground="#8F9AAA" FontSize="11" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,7,0" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButton}" Click="OnZoomOutClick" ToolTip="{local:UiText Уменьшить}" AutomationProperties.Name="{local:UiText Уменьшить}">
                        <Path Width="13" Height="13" Stroke="#DCE3ED" StrokeThickness="1.6" StrokeStartLineCap="Round" Data="M2,6.5 L11,6.5" />
                    </Button>
                    <TextBlock x:Name="ZoomText" Width="43" Foreground="#B9C3D1" FontSize="11" TextAlignment="Center" VerticalAlignment="Center" />
                    <Button Style="{StaticResource IconButton}" Click="OnZoomInClick" ToolTip="{local:UiText Увеличить}" AutomationProperties.Name="{local:UiText Увеличить}">
                        <Path Width="13" Height="13" Stroke="#DCE3ED" StrokeThickness="1.6" StrokeStartLineCap="Round" Data="M2,6.5 L11,6.5 M6.5,2 L6.5,11" />
                    </Button>
                    <Button x:Name="FitButton" Style="{StaticResource PreviewButton}" Click="OnFitClick" Content="{local:UiText По размеру окна}" ToolTip="{local:UiText По размеру окна}" />
                    <Button Style="{StaticResource PreviewButton}" Click="OnActualSizeClick" Content="100%" ToolTip="100%" />
                    <Button x:Name="FullscreenButton" Style="{StaticResource IconButton}" Click="OnFullscreenClick" ToolTip="{local:UiText На весь экран}" AutomationProperties.Name="{local:UiText На весь экран}">
                        <Path Width="14" Height="14" Stroke="#DCE3ED" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeLineJoin="Round" Data="M1,5 L1,1 L5,1 M9,1 L13,1 L13,5 M13,9 L13,13 L9,13 M5,13 L1,13 L1,9" />
                    </Button>
                    <Button Style="{StaticResource PreviewPrimaryButton}" Margin="7,0,4,0" Click="OnMarkupClick" Content="{local:UiText Разметка}" />
                    <Button Style="{StaticResource IconButton}" Click="OnCloseClick" ToolTip="{local:UiText Закрыть просмотр}" AutomationProperties.Name="{local:UiText Закрыть просмотр}">
                        <Path Width="11" Height="11" Stroke="#DCE3ED" StrokeThickness="1.6" StrokeStartLineCap="Round" Data="M1,1 L10,10 M10,1 L1,10" />
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="292" /></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="12,12,6,12" Background="#0A0D11" CornerRadius="10" ClipToBounds="True">
                    <ScrollViewer x:Name="ImageScroller" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                  PanningMode="Both" Background="Transparent" PreviewMouseWheel="OnImageMouseWheel" SizeChanged="OnImageViewportSizeChanged">
                        <Grid x:Name="ZoomHost" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <Grid.LayoutTransform><ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1" /></Grid.LayoutTransform>
                            <Image x:Name="PreviewImage" Stretch="Fill" SnapsToDevicePixels="True" />
                        </Grid>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="1" Margin="6,12,12,12" Padding="12" Background="#171B22" CornerRadius="10">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                        <DockPanel Margin="1,0,1,10">
                            <Button DockPanel.Dock="Right" Style="{StaticResource IconButton}" Click="OnAddCommentClick"
                                    ToolTip="{local:UiText Добавить комментарий}" AutomationProperties.Name="{local:UiText Добавить комментарий}">
                                <Path Width="14" Height="14" Stroke="#DCE3ED" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M1,1 L10,1 L10,8 L6,8 L3,11 L3,8 L1,8 Z M12,5 L12,13 M8,9 L16,9" />
                            </Button>
                            <TextBlock VerticalAlignment="Center" Foreground="#EEF2F8" FontWeight="SemiBold" Text="{local:UiText Комментарии}" />
                        </DockPanel>

                        <Grid Grid.Row="1">
                                <ListBox x:Name="CommentList" ItemsSource="{Binding Comments}" Background="Transparent" BorderThickness="0"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0" />
                                            <Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" />
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><ContentPresenter /></ControlTemplate></Setter.Value></Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate></ListBox.ItemsPanel>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,9" Padding="9" CornerRadius="10" Background="#202630">
                                                <Grid>
                                                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="30" /></Grid.ColumnDefinitions>
                                                    <StackPanel Orientation="Horizontal" Margin="1,0,5,7">
                                                        <TextBlock Foreground="#7AB8FF" FontSize="11" FontWeight="SemiBold" Text="{Binding Label}" />
                                                        <TextBlock Margin="6,0,0,0" Foreground="#8F9AAA" FontSize="11" Text="{Binding Relation}" TextTrimming="CharacterEllipsis" />
                                                    </StackPanel>
                                                    <Button Grid.Column="1" Style="{StaticResource IconButton}" Width="28" Height="28" Padding="7" Margin="0,-5,-5,0"
                                                            Click="OnDeleteCommentClick" Tag="{Binding}" ToolTip="{local:UiText Удалить комментарий}" AutomationProperties.Name="{local:UiText Удалить комментарий}">
                                                        <Path Width="10" Height="10" Stroke="#BFC8D6" StrokeThickness="1.5" StrokeStartLineCap="Round" Data="M1,1 L9,9 M9,1 L1,9" />
                                                    </Button>
                                                    <TextBox Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource PreviewTextBox}" MinHeight="54" MaxHeight="150"
                                                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                                             Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <TextBlock x:Name="EmptyCommentsText" Margin="8,12" Foreground="#8F9AAA" TextWrapping="Wrap" Text="{local:UiText Нет комментариев}" IsHitTestVisible="False" />
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
