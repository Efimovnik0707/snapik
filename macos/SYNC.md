# Синхронизация Windows → macOS

Источник истины: Windows-версия в `src/`. Mac-порт в `macos/` догоняет её порциями («синхронизациями»). Оба живут в одном git-репозитории (`Efimovnik0707/snapik`), поэтому «что изменилось на Windows» всегда выводится из `git diff`.

## Правила

1. Любое изменение Windows-версии коммитится в этот репозиторий (можно пачкой в конце сессии). Без коммита Mac-порт изменения не увидит.
2. Каждая синхронизация начинается тегом `mac-sync-base-N` на том коммите Windows-кода, который уже перенесён. Разница `git diff mac-sync-base-N HEAD -- src tests tasks` = что переносить.
3. Перенос: спека дельты (если изменение крупное) → исполнитель в `macos/Sources` → CI на macOS-раннере (сборка, 99+ тестов, smoke, DMG) → новый тег `mac-sync-base-N+1` и запись в журнал ниже.
4. Команда для запуска: «синхронизируй мак» (или «sync mac»). Агент сам находит тег, строит diff и делает перенос, в конце даёт ссылку на DMG.
5. Новые строки интерфейса на Windows добавляются в `UiLanguage.cs`; на Mac они попадают в `macos/Sources/SnapikCore/Settings/UiLanguage.swift` в том же порядке. Формат `session.json` и настроек менять только синхронно в обеих версиях.

## Журнал

| № | База (тег) | Что перенесено | Релиз macOS |
|---|---|---|---|
| 0 | `mac-sync-base-0` = `6db8127` | первичный порт всей Windows-версии на 9 сентября 02:55 | `macos-v0.1.0-22` |
| 1 | `mac-sync-base-1` = `4771a42` | панель цвета и толщины (палитра 12 цветов, HEX, ползунок 1–16 px), меню «…» без разделителя с отметкой выбранного, подсказки с клавишами, ввод текста инструмента «Текст», smoke-проверки | `macos-v0.1.0-24` |
| 2 | `mac-sync-base-2` = `e588ad0` (Windows HEAD на 9 сентября, 10 коммитов) | универсальный перехват Cmd+V/Ctrl+V и поштучная вставка, многоразовый пакет, отзвук получателя, связанные комментарии (пины, N), стили стрелок, окно предпросмотра с панелью комментариев, стопка-гармошка, звук, автосохранение, настройки, метки после Z, диагностика в startup.log | `macos-v0.1.0-32` |
| 3 | `mac-sync-base-3` = `1c7d727` (Windows 1.4.0 плюс переименование SnapBrief → Snapik; спека `SPEC-DELTA-3.md`, заметки волны 0 `WAVE0-NOTES-3.md`) | семь новых полей разметки и их сериализация; правила Core (`SentCaptureRules`, `StripResizeGeometry`, `SaveNaming`, `ColorConversion`, `TextMarkMetrics`, `HotkeyRules`, `SettingsMigration`); 23 ключа настроек с миграцией громкости и лечением битого id на старте; 213 новых строк интерфейса; лента в целевом состоянии ТЗ №4 (ширина 224, капсула, «Поверх других окон», лимит 26 на всех точках добавления, тосты, скрытие только от своего захвата); редактор раунда (фигуры и заливки, спектр и пипетка, узоры штриха, кегль подписи, панель комментариев, шпаргалка клавиш); шесть тем и двенадцать акцентов вместо светлой темы; окно настроек с вкладкой «Вид» (620×520), окно сохранения пакета, мастер первого запуска со слайдами «Как пользоваться»; три mp3 вместо двух wav с громкостью и гейнами; окно предпросмотра удалено — карточка открывается сразу в разметке | — |
| 4 | `mac-sync-base-4` = `d84fbb0` (Windows 1.5.0, 38 коммитов; спека `SPEC-DELTA-4.md`, заметки волны 0 `WAVE0-NOTES-4.md`) | вид снимка (`CaptureKind`, `monitorCount`, `title`, чипы карточки, подпись в разметке, «Снимок A — весь экран.» в тексте задания); снимок всего экрана и импорт файла ложатся в ленту (клавиша «весь экран» больше не пишет PNG мимо ленты, на диск кладёт автосохранение); редактор с масштабом (колесо, Shift, Ctrl+колесо, пробел, переключатель «По ширине · 35 %» ↔ «1:1», `EditorGeometry`); геометрия ленты 244/20/0 (панель 204, карточка 168, тень 24/5); миграция настроек до версии 2 (`light` → `dark`, пороги на каждое правило); `hasOutline` только читается (`LegacyHasOutline`); пометка вставленных снимков `Sent` (T-5); очистка сессий C-15; лупа пипетки и «+» в палитру; 11 новых пар `UiLanguage`; исправления из ревью раунда | `macos-v1.5.0` |

Не перенесено синхронизацией №4 и ждёт следующей: `FrameCopy.Detach` не переносится вовсе (`SPEC-DELTA-4.md` §4.4 — на Mac `CGImage` владеет своими данными; перенесено только тест-намерение «картинка с диска кодируется с фонового потока»); интерактивная проверка на живом Mac (многомониторный захват, реальная вставка в Claude/Codex, ощущение интерфейса) — `README.md` «Что проверено и что нет». База следующей синхронизации — Windows HEAD на момент её начала.
