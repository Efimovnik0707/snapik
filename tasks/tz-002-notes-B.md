# Заметки дорожки B (ТЗ №2, лента снимков)

Ветка `tz002-B`, базовый коммит `b14341d`. Порядок работ из `tasks/tz-002-plan.md`, раздел 3. После каждого коммита прогон `scripts/build.ps1` (тесты и `--smoke-test`).

## 1. `windows: a capture plays a single sound` (F1 в ленте)

Сделано:

- Из `NotifyCopied` (`EdgeStackWindow.Saving.cs`) убран `UiSoundService.Copied`, метод остался балуном «Снимки скопированы». Звук «скопировано» звучит теперь ровно в одном месте, в `CopyPackageAsync` (`EdgeStackWindow.xaml.cs`), то есть только при явном «Копировать пакет».
- Захват, импорт, удаление, реордер, восстановление и тик сохранения буфера балун показывают, но молчат.
- `UiSoundService.Capture(_settings)` поднят выше `SaveAndCopyCommittedPackageAsync()` в `CaptureLoopAsync`: затвор больше не ждёт записи в буфер и совпадает с моментом захвата.
- Файл звука, гейн и дефолт громкости не трогались, это зона дорожки A.

Проверено: `scripts/build.ps1` зелёный, 127 тестов (41 Imaging, 25 Core, 61 Windows), smoke прошёл. Юнит-теста на звук нет, звук это побочный эффект.

Живьём не проверено: что захват даёт ровно один звук, что «Копировать пакет» звучит, а удаление и импорт молчат. Проверяется по сценарию из ТЗ на собранной версии.

Изменение формата: нет.

## 2. `windows: the strip opens the editor and the preview window is gone` (D1 в ленте)

Сделано:

- Класс `UiTextExtension` перенесён из конца `CapturePreviewWindow.xaml.cs` в самый конец `UiLanguage.cs` отдельным классом ниже таблицы (через него во всех XAML работает `{local:UiText ...}`, без переноса проект не собирается).
- `OnOpenCaptureClick` переписан и переехал в `EdgeStackWindow.xaml.cs` к обработчикам списка: клик по карточке сразу зовёт `OverlayEditorWindow.EditExistingAsync`, блок просмотра с `SuspendTopmost` убран (редактор полноэкранный, `HideForCapture()` прячет все окна приложения). Снятие флага `Sent`, перенумерация, повторный экспорт и запрос следующего снимка сохранены дословно.
- Туда же переехали `OnCaptureThumbMouseEnter` и `OnCaptureListMouseWheel`.
- Удалены `CapturePreviewWindow.xaml`, `CapturePreviewWindow.xaml.cs` (вместе с `PersistPreviewChangesAsync` в партиале) и `EdgeStackWindow.Preview.cs`. Правок в `csproj` не потребовалось, файлы подключались глобом.
- Из `SmokeTestRunner.cs` убран блок `RunPreviewProbe` целиком.
- Из `UiLanguage.cs` удалены восемь пар просмотра («Просмотр снимка», «По размеру окна», «Увеличить», «Уменьшить», «На весь экран», «Вернуть размер», «Разметка», «Закрыть просмотр»). Пары про комментарии («Комментарии», «Нет комментариев», «Комментарий к снимку», «К снимку», «К отметке», «Добавить комментарий», «Удалить комментарий») оставлены, их берёт дорожка C под D2.
- Сигнатура `EditExistingAsync` не менялась, «открыт из ленты» редактор по-прежнему отличает по своему полю `_isNew`.

Покрытие взамен умершего пробника: в `tests/SnapBrief.Core.Tests/ExtendedCommentsTests.cs` добавлен чистый Core-тест над `CaptureLabels.ForNotedAnnotations` (пустая заметка номера не занимает, после удаления нумерация закрывает дыру). Список из 300 комментариев там уже проверялся раньше. Проверка границ окна просмотра (`CalculatePreviewBounds`) умерла вместе с окном, как и предусмотрено планом. Полноценный пробник панели комментариев делает дорожка C.

Проверено: `scripts/build.ps1` зелёный, 128 тестов (41 Imaging, 26 Core, 61 Windows), smoke прошёл. Grep по `CapturePreviewWindow`, `RunPreviewProbe`, `PreviewButton`, `UiTextExtension` мёртвых ссылок в исходниках не даёт.

Живьём не проверено: что клик по карточке открывает редактор со всеми объектами и пилюлями и что после правки пакет пересобирается.

Изменение формата: нет.
